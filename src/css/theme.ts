import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#FFF",
    },
    text: {
      primary: "#010A1C",
    },
    background: {
      default: "#FFF",
    },
  },
});

export default theme;
